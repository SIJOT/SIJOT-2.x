<?php

namespace App\Http\Middleware;

use Closure;

class groupMiddleware
{
    /**
     * Handle an incoming request. - user management middleware
     *
     * @param  \Illuminate\Http\Request  $request
     * @param  \Closure  $next
     * @return mixed
     */
    public function handle($request, Closure $next)
    {
        if () {
            if () {

            }
        }
        return $next($request);
    }
}
